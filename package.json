{
  "name": "quiz-parametres-coupe",
  "version": "0.1.0",
  "private": true,
  "description": "Exerciseur web auto-corrigé de paramètres de coupe (tournage, fraisage, perçage)",
  "type": "module",
  "scripts": {
    "test": "node --disable-warning=ExperimentalWarning --test \"tests/**/*.test.js\"",
    "dev": "wrangler d1 migrations apply quiz-parametres-coupe --local && wrangler dev",
    "deploy": "wrangler d1 migrations apply quiz-parametres-coupe --remote && wrangler deploy"
  },
  "engines": {
    "node": ">=22.13"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "wrangler": "4.135.0"
  }
}
